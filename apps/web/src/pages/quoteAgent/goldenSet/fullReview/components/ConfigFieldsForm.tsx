import type { ConfigurationField, FrozenEvidence, PackageItem } from "../types";

export function ConfigFieldsForm({ value, items, evidence, onChange }: { value: ConfigurationField[]; items: PackageItem[]; evidence: FrozenEvidence[]; onChange(index: number, field: ConfigurationField): void }) {
  return <fieldset><legend>关键配置</legend>{value.length === 0 && <p className="full-review-help">本任务没有预置配置字段。</p>}{value.map((field, index) => <article className="full-review-item" key={index}><div className="full-review-form-grid"><label>字段名<input value={field.field_key} onChange={(event) => onChange(index, { ...field, field_key: event.target.value })} /></label><label>所属产品<select value={field.item_id ?? ""} onChange={(event) => onChange(index, { ...field, item_id: event.target.value || null })}><option value="">文档级 / 未指定</option>{items.map((item) => <option key={item.gold_item_id} value={item.gold_item_id}>{item.item_name || item.gold_item_id}</option>)}</select></label><label>值<input value={field.value ?? ""} onChange={(event) => onChange(index, { ...field, value: event.target.value || null })} /></label><label>单位<input value={field.unit ?? ""} onChange={(event) => onChange(index, { ...field, unit: event.target.value || null })} /></label><label>选项<input value={field.option ?? ""} onChange={(event) => onChange(index, { ...field, option: event.target.value || null })} /></label><label>证据<select multiple value={field.evidence_refs} onChange={(event) => onChange(index, { ...field, evidence_refs: Array.from(event.target.selectedOptions, (option) => option.value) })}>{evidence.map((entry) => <option key={entry.evidence_id} value={entry.evidence_id}>{entry.evidence_id}</option>)}</select></label></div></article>)}</fieldset>;
}
