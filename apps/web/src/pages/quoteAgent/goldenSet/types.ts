export type GoldenLayer = "product_package" | "erp_identity";
export type GoldenTask = { sample_id: string; layer: GoldenLayer; strata: Record<string, unknown>; selection_reasons: string[]; evidence: { evidence_id: string; source: Record<string, unknown>; erp_candidates: Array<{ evidence_id: string; company: string; part_num: string; product_name: string | null; prod_code: string | null; has_bom: boolean | null }>; note: string }; annotation: unknown; revision: number };
